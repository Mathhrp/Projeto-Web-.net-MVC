@model IEnumerable<_153264_152728.Models.LanPro>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Produtos.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Produtos.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LanProId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LanProId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LanProId })
        </td>
    </tr>
}

</table>
