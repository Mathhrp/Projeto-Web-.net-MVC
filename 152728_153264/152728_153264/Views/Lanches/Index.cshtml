@model IEnumerable<_152728_153264.Models.Lanche>

@{
    ViewBag.Title = "Index";
}

<h2>Lanches</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Preco)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Preco)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.LancheID }) |
                @Html.ActionLink("Details", "Details", new { id = item.LancheID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.LancheID })
            </td>
        </tr>
    }

</table>
