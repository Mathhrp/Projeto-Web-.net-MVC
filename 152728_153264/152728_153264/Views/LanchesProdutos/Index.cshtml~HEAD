@model IEnumerable<_152728_153264.Models.LancheProduto>

@{
    ViewBag.Title = "Index";
}

<h2>Lanche Produto</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Lanche.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Produto.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.qtde)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Lanche.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Produto.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.qtde)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LancheProdutoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LancheProdutoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LancheProdutoId })
        </td>
    </tr>
}

</table>
